<DeliveryPage>:
    name: 'delivery'
    GridLayout
        cols: 2
        Button
            text: 'Back to home'
            on_press: root.manager.current = 'home'

        Button
            text: 'Back to home'
            on_press: root.manager.current = 'home'

        Button
            text: 'Back to home'
            on_press: root.manager.current = 'home'

        Button
            text: 'Back to home'
            on_press: root.manager.current = 'restaurant'
    # BoxLayout
    #     Button:
    #         text: 'Back to Home'
    #         on_press: root.manager.current = 'home'